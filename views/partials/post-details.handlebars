<div class="card">
    <div class="card-header">
        <h3>{{title}}</h3>
        <p>Author: {{user.name}} on: {{format_time created_at}}</p>
    </div>
    <div class="card-body">
        <p>
            {{body}}
        </p>
    </div>
</div>